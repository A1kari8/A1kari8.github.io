---
title: "[形式语言与自动机] 正则语言与正则表达式"
published: 2026-5-30 16:25:00
tags: [形式语言与自动机]
category: 形式语言与自动机
draft: false
---

## 正则语言的定义

正则语言是在有限字母表 $\Sigma$ 上的一个集合，其中包含的元素是所有能被一个**正则表达式/DFA/NFA**识别的字符串，正则语言是$\Sigma^*$的一个子集

## 正则表达式的定义

正则表达式是一个字符串，使用特定的语法规则来描述一个正则语言。正则表达式和DFA、NFA之间是等价的，即它们识别的正则语言是相同的

$$
\text{正则表达式} \Leftrightarrow \text{DFA} \Leftrightarrow \text{NFA}
$$

正则表达式由以下基本元素组成，且以下每个元素本身也是一个正则表达式：

- **空集**：表示一个不包含任何字符串的语言，通常用符号 $\emptyset$ 表示
- **空字符串**：表示一个只包含空字符串的语言，通常用符号 $\epsilon$ 表示
- **单个符号**：对于每个输入符号 $a \in \Sigma$，表示一个只包含字符串 $a$ 的语言
- **连接**：如果 $R$ 和 $S$ 是正则表达式，那么 $RS$ 表示连接操作，表示由 $R$ 中的字符串连接 $S$ 中的字符串组成的语言，或多次连接，例如 $R^n$ 表示由 $R$ 中的字符串连接 $n$ 次组成的语言，$R^0=\{\epsilon\}$
- **选择**：如果 $R$ 和 $S$ 是正则表达式，那么 $R|S$ 表示选择操作，表示由 $R$ 中的字符串或 $S$ 中的字符串组成的语言
- **闭包**：如果 $R$ 是一个正则表达式，那么 $R^*$ 表示闭包操作，表示由 $R$ 中的字符串重复零次或多次组成的语言

一个正则表达式通过有限次上述操作后生成的仍然是一个正则表达式，因此正则表达式可以**递归定义**

:::tip

正闭包：$R^+$ 表示由 $R$ 中的字符串重复**一次或多次**组成的语言，即 $R^+ = RR^*$

克林闭包：$R^*$ 表示由 $R$ 中的字符串重复**零次或多次**组成的语言，即 $R^* = \{\epsilon\} \cup R^+$

运算顺序：括号 > 闭包 > 连接 > 选择

:::

## 例子

设计一个正则表达式匹配所有包含110但不能包含111的二进制字符串

可以使用以下正则表达式：

$$
(0∣10)^∗110(0∣10∣01)^∗
$$

## DFA/NFA转正则表达式

e.g. 现有一DFA，状态转移表如下：

| 状态 | 输入0 | 输入1 |
|---|---|---|
| $\rightarrow q_0$ | $q_1$ | $q_0$ |
| $q_1$ | $q_2$ | $q_0$ |
| ${}^*q_2$ | $q_2$ | $q_2$ |

### 状态消除法

1. 添加唯一的开始状态和接受状态(实际上这么处理完已经变NFA了)：

| 状态 | 输入0 | 输入1 | $\epsilon$ |
|---|---|---|---|
| $\rightarrow S$ | - | - | $q_0$ |
| $q_0$ | $q_1$ | $q_0$ | - |
| $q_1$ | $q_2$ | $q_0$ | - |
| $q_2$ | $q_2$ | $q_2$ | $F$ |
| ${}^*F$  | - | - | - |

1. 逐个消除中间状态：

经过步骤1后，中间状态是$q_0$和$q_1$，我们可以先消除$q2$：

关注要消除的状态的**入边**、**自环**和**出边**，将其替换为一个新的正则表达式，公式为：

$$
\text{入边表达式} \cdot (\text{自环表达式})^* \cdot \text{出边表达式} + \text{入边来源到出边目标原有表达式}
$$

- $q_2$入边：$q_1 \xrightarrow{0} q_2$，则表达式为$0$
- $q_2$自环：$q_2 \xrightarrow{0} q_2, q_2 \xrightarrow{1} q_2$，则表达式为$0+1$
- $q_2$出边：$q_2 \xrightarrow{\epsilon} F$, 则表达式为$\epsilon$

所以消去$q_2$后，只有一个来源和一个目标，则只有$q_1$到$F$的边，$q_1$到$F$的表达式为：

$$
0(0+1)^*\epsilon + \text{无} = 0(0+1)^*
$$

当前状态转移为：

$$
S \xrightarrow{\epsilon} q_0, \quad q_0 \xrightarrow{0} q_1, \quad q_1 \xrightarrow{1} q_0, \quad q_0 \xrightarrow{1} q_0, \quad q_1 \xrightarrow{0(0+1)^*} F
$$

消$q_1$:

- 入边：$q_0 \xrightarrow{0} q_1$，则表达式为$0$
- 自环：$q_1$没有自环，则表达式为$\epsilon$
- 出边：$q_1 \xrightarrow{0(0+1)^*} F, q_1 \xrightarrow{1} q_0$，则表达式有$0(0+1)^*$与$1$

则$q_0$到$F$的表达式为：

$$
0 \cdot (\epsilon)^* \cdot 0(0+1)^* + \text{无} = 00(0+1)^*
$$

$q_0$到$q_0$的表达式为：

> 注意$q_0$到$q_0$已存在转移(自环)，不要忘记并上

$$
0 \cdot (\epsilon)^* \cdot 1 + 1 = 01 + 1
$$

此时，状态转移为：

$$
S \xrightarrow{\epsilon} q_0, \quad q_0 \xrightarrow{01 + 1} q_0, \quad q_0 \xrightarrow{00(0+1)^*} F
$$

消$q_0$:

- 入边：$S \xrightarrow{\epsilon} q_0$，则表达式为$\epsilon$
- 自环：$q_0 \xrightarrow{01 + 1} q_0$，
- 出边：$q_0 \xrightarrow{00(0+1)^*} F$，则表达式为$00(0+1)^*$

则$S$到$F$的表达式为：

$$
\epsilon \cdot (01 + 1)^* \cdot 00(0+1)^* + \text{无} = (01 + 1)^*00(0+1)^*
$$

则结果为：$(01 + 1)^*00(0+1)^*$
